apiVersion: apps/v1
kind: Deployment
metadata:
  name: mlflow
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mlflow
  template:
    metadata:
      labels:
        app: mlflow
    spec:
      nodeSelector:
        node-role: training
      containers:
        - name: mlflow
          image: us-central1-docker.pkg.dev/sodium-pager-461309-p3/mlflowserver/mlflow-custom:latest
          ports:
            - containerPort: 5000
          env:
          - name: GOOGLE_APPLICATION_CREDENTIALS
            value: /app/key.json
          - name: BUCKET_NAME
            valueFrom:
              secretKeyRef:
                name: gcp-secrets
                key: bucket-name
          - name: BLOB_NAME
            valueFrom:
              secretKeyRef:
                name: gcp-secrets
                key: blob-name
          volumeMounts:
          - name: gcp-key
            mountPath: /app/key.json
            subPath: key.json
          command: ["mlflow", "server"]
          args:
            - "--backend-store-uri=postgresql://mlflow_user:mlflow123@34.55.49.20:5432/mlflow_db"
            - "--default-artifact-root=gs://ml-artifacts-tutorial"
            - "--host=0.0.0.0"
      volumes:
        - name: gcp-key
          secret:
            secretName: gcp-key-secret
